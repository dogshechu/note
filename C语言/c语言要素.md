1，预处理指令
 以#开头
 在c程序编译之前修改程序文本，
2， main函数(每一个c程序有且只有一个主函数)
  `int main()`
  `{`
  `//函数体`
  `return 0;`
  `}`
3，标识符
    就是c 语言已经定义好的词。
     自己定义：只能有英语字母，数字，下画线组成，且第一个字符只能是字母或下画线。不能用c语言已经定义好的词。
4，数据类型
 ![[数据类型.jpg]]
 还有有符号和无符号之分，要注意。
5，变量和常量
     1，常量
          1，整型常量
             1，十进制
             2，八进制
             3，十六进制
         2，浮点型常量
             1，十进制小数形式
             2，指数形式
         3，字符型常量
             char 就是一个字符，’a‘,'1',':','?'等等。（ASCII码表 ）
          4，功能型变量（转义字符）
             ![[数据结构.jpg]]
          5，字符串常量   ”  “
          6，符号常量   
      2，变量
         定义：值会改变的量。
         内容：变量名，变量值，变量地址。
            ![[结构声明.jpg]]
                ![[结构声明 1.jpg]]  
6，语句
      1，输入语句`putchar();printf();`
      2，输出语句`getchar();scanf();`
      3，表达式语句
      4，返回语句
 7，注释
      1，块注释--`/*     */`可以多行
      2，行注释--//只能一行
  8，运算符和表达式
      运算符：`+，-，*，/，++，--，%，-=，+=，*=，/=，%=`
      位运算：`&，|，~，<<,>>,^`
  9，变量和赋值
       形式：int sum;
      int sum1,sum2;
      float num;
      char a;等等
     赋值运算符是把右边的赋给左边的变量，`a=b*2;`类型不一样时可能会产生误差。（隐式转换法）
     强制转换法  （int )(grade);或int（grade)，（int）grade-- 一般我用这个;
 